export function GameFooter() {
  return (
    <footer className="relative z-20 flex shrink-0 items-center gap-2 px-4 py-2 md:px-8 md:py-2.5">
      <span className="flex h-7 w-7 items-center justify-center rounded-full bg-[#4caf50] text-xs font-bold text-white shadow-md">
        A
      </span>
      <span className="text-sm font-semibold tracking-wider text-white drop-shadow-md">
        Select
      </span>
    </footer>
  );
}
